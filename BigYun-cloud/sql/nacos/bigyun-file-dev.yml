file:
  domain: ${FILE_DOMAIN:http://127.0.0.1:9300}
  path: ${FILE_PATH:D:/bigyun/uploadPath}
  prefix: /statics

minio:
  url: ${MINIO_URL:http://127.0.0.1:9000}
  accessKey: ${MINIO_ACCESS_KEY:change_me}
  secretKey: ${MINIO_SECRET_KEY:change_me}
  bucketName: ${MINIO_BUCKET:bigyun}
