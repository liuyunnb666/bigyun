# Provider Capability Guide

Use Provider configuration to add or switch third-party capabilities such as LLM, OCR, face recognition, storage, SMS, email, translation, and payment.

- Add provider metadata.
- Define visible fields and secret fields.
- Add API templates for request path, method, headers, auth, and response parsing.
- Bind provider models to capability codes.
- Test in a local environment with private credentials.